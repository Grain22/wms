<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--&lt;!&ndash; 别名的定义 &ndash;&gt;-->
    <!--<typeAliases>-->
        <!--&lt;!&ndash; 批量定义别名 ，指定包名，自动扫描包中的类，别名即为类名，首字母大小写无所谓&ndash;&gt;-->
        <!--<typeAlias type="sel.bean.BeanTest" alias="cat"/>-->
        <!--<package name="sel.bean"/>-->
    <!--</typeAliases>-->

    <!-- 数据库连接用数据库连接池 -->
    <settings>
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>
    <mappers>
        <!-- 通过扫描包的方式来进行批量加载映射文件 -->
        <package name="sel"/>
    </mappers>
</configuration>